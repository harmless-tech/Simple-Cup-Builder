[options]
git_update_timer = 15 # In minutes.
parallel_builds = false # This could break certin build processes. Could also overwhelm stuff. #TODO Implement!

[process]
windows = "pwsh" # Options: cmd, pwsh (If pwsh is installed and on the path)
linux = "/bin/bash" # Options: Any linux shell (That is installed)
macos = "/bin/bash" # Options: Any macos shell (That is installed)

build_files = [
    "git_build_hta.toml"
]

[add_path]
all = [
]
windows = [
]
linux = [
]
macos = [
]

[add_env] # SCB_VERSION, BUILD_TRIGGER_TIME

[alias] # This replaces anything in the command.
