# Uses the build file in the git project. If this option is used all other options in this file are ignored.
use_internal_build_file = ""

[info]
name = "HTA Build"
build_file_version = "1"

[git]
url = ""
branch = "master"
clean_build = false

clean_before_build = [
]

[cmds]
wrk_dir = "" # Based off of the git created directory.
test_cmds = [ # These cmds must exit with a code 0 for the build to be started.
]
cmds = [
    "",
    ""
]

# These add options may not override options in the scb.toml file.
[add_path]
all = [
]
windows = [
]
linux = [
]
macos = [
]

[add_env] # PROJECT_NAME, BUILD_FILE_VERSION, BUILD_COUNT, BUILD_START_TIME

[alias]
