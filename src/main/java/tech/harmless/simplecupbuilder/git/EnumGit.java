package tech.harmless.simplecupbuilder.git;

//TODO Change this enum to an enum of general status codes? (EnumStatus)

/**
 * Please use EnumStatus.
 */
@Deprecated(since = "0.0.1", forRemoval = true)
public enum EnumGit {
    NULL,
    FAILURE,
    SUCCESS,
    NO_CHANGE,
    CHANGE
}
